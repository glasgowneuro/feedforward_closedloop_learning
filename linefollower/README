This implements a simple error driven line follower with the help of the ENKI robot simulator.
To run:

1) Install enki:
Clone 
https://github.com/berndporr/enki/
and install the library with
qmake
make
sudo make install

2) Compile the program:
make
qmake
./linefollower
